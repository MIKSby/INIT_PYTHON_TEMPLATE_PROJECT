**INIT TEMPLATE PYTHON PROJECT WITH POETRY AND GIT-HOOKS**

| ``poetry run init`` - init git hooks
| ``poetry run lint`` - start python linters(mypy, pycodestyle, flake8, pylint)
| ``poetry run pre_push`` - check with safety and bandit
